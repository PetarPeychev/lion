(
    quote (str) def
    quote (quot) def

    (quot uncons (x) def string) (symname) def
    (symname quote ("_docs") cat join) (docname) def
    (docname uncons (x) def symbol quote) (docsym) def

    quot
    str quote
    docsym
)
(docs) def